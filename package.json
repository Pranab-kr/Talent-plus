{
  "name": "talent-plus",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "bun install --cwd backend && bun install --cwd frontend && bun run --cwd frontend build",
    "start": "bun run --cwd backend start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "backend": "^0.0.0"
  }
}
